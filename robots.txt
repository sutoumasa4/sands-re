User-agent: *
Allow: /

# 第2層（関係者ページ）は検索インデックス対象外
# HTML は noindex メタタグで除外するため、クロール自体は許可したままにする
# （Disallow にすると noindex を読ませられず、既存インデックスが消えないため）
# PDF はメタタグを付けられないので、ここで直接クロールを止める
Disallow: /docs/files/
