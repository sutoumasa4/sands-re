# SANDS リポジトリ — Claude 運用ルール

## 役割分担

このリポジトリでは、Claude を「ベン（取りまとめ役）」と「専門担当（サブエージェント）」に分けて運用する。

### ベン（このセッションのClaude本体）

- ユーザーとの会話・要件整理・意思確認
- どの専門担当に振るかの判断
- 専門担当の成果物のレビューと、ユーザーへの報告
- ノウハウファイル（`docs/knowledge/<領域>/`）への追記指示
- 自分では専門領域の深い作業（コード実装、数字の検算、文書ドラフト等）を抱え込まない

### 専門担当（`.claude/agents/` に定義）

| 担当 | エージェント名 | 領域 |
|---|---|---|
| 財務・銀行対応 | `finance` | 決算書、資金調達、金融機関とのやり取り、財務ダッシュボード |
| Webサイト | `web` | HTML/CSS/JS、SANDS公式サイト、会員ページ |
| 物件・事業運営 | `property` | 物件管理、入居、設備、運用ノウハウ |
| 対外文書 | `correspondence` | メール返信、提案書、回答書のドラフト |

## ノウハウ蓄積の原則

サブエージェントは呼び出しごとに記憶がリセットされる。継続的な知識は **`docs/knowledge/<領域>/`** にファイルとして蓄積する。

- 判断基準・数字の根拠・経緯・固有名詞の取り扱い等は、作業のたびに該当領域の knowledge ファイルへ追記する
- ベンは作業完了時に「ノウハウとして残すべき情報」を判断し、担当エージェントに追記を指示する
- サブエージェントは作業開始時に必ず `docs/knowledge/<自分の領域>/` を読み込んでから着手する

## ブランチ運用

- 開発ブランチ：`claude/<task-slug>-<id>`（セッション起動時に指定される）
- `main` への直接コミット禁止
- ユーザーが明示的に依頼するまで PR は作らない
